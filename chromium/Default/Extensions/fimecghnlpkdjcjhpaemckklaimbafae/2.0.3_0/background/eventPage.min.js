"use strict";chrome.runtime.onInstalled.addListener(function(details){if("install"==details.reason)chrome.tabs.create({url:"tab.html#!/settings"});else if("update"==details.reason){chrome.runtime.getManifest().version;chrome.tabs.create({url:"tab.html#!/changelog"})}});